2. smm icons 
3. conact us pages m
4. Zoom in out product images on product page not working





//winix technial detail and addi information
 <span class="font-bold text-xl text-neutral-800 block mt-3">Product Information</span>
      <div class="productInfo-container mt-3">
        <!-- LEFT SIDE -->
        <div class="productInfo-left">
          <!-- FEATURES & SPECS -->
          <div class="accordion" (click)="toggle('features')">
            <h3 class="text-md sm:text-lg text-neutral-800">Features & Specs</h3>
            <span [innerHTML]="isOpen.features ? '&#9650;' : '&#9660;'"></span>
          </div>

          <div class="accordion-content" *ngIf="isOpen.features">
            <div class="row" *ngFor="let key of objectKeys(product.productInformation?.featuresAndSpecs || {})">
              <div class="label">{{ formatKey(key) }}</div>
              <div class="value">{{ product.productInformation?.featuresAndSpecs?.[key] }}</div>
            </div>
          </div>


          <!-- MEASUREMENTS -->
          <div class="accordion" (click)="toggle('measurements')">
            <h3 class="text-md sm:text-lg text-neutral-800">Measurements</h3>
            <span [innerHTML]="isOpen.measurements ? '&#9650;' : '&#9660;'"></span>
          </div>

          <div class="accordion-content" *ngIf="isOpen.measurements">
            <div class="row" *ngFor="let key of objectKeys(product.productInformation?.measurements || {})">
              <div class="label">{{ formatKey(key) }}</div>
              <div class="value">{{ product.productInformation?.measurements?.[key] }}</div>
            </div>
          </div>


          <!-- USER GUIDE -->
          <div class="accordion" (click)="toggle('userGuide')">
            <h3 class="text-md sm:text-lg text-neutral-800">User guide</h3>
            <span [innerHTML]="isOpen.userGuide ? '&#9650;' : '&#9660;'"></span>
          </div>

          <div class="accordion-content" *ngIf="isOpen.userGuide">
            <div class="row" *ngFor="let key of objectKeys(normalizedUserGuide)">
              <div class="label">{{ formatKey(key) }}</div>
              <div class="value">{{ normalizedUserGuide[key] }}</div>
            </div>
          </div>


          <!-- ADDITIONAL DETAILS -->
          <div class="accordion" (click)="toggle('additional')">
            <h3 class="text-md sm:text-lg text-neutral-800">Additional details</h3>
            <span [innerHTML]="isOpen.additional ? '&#9650;' : '&#9660;'"></span>
          </div>

          <div class="accordion-content" *ngIf="isOpen.additional">
            <div class="row" *ngFor="let key of objectKeys(product.productInformation?.additionalDetails || {})">
              <div class="label">{{ formatKey(key) }}</div>
              <div class="value">{{ product.productInformation?.additionalDetails?.[key] }}</div>
            </div>
          </div>

        </div>
        <!-- RIGHT SIDE ITEM DETAILS -->
        <div class="productInfo-right">
          <div class="accordion" (click)="toggle('itemDetails')">
            <h3 class="text-md sm:text-lg text-neutral-800">Item details</h3>
            <span [innerHTML]="isOpen.itemDetails ? '&#9650;' : '&#9660;'"></span>
          </div>

          <div class="accordion-content" *ngIf="isOpen.itemDetails">

            <div class="row" *ngIf="product.itemDetails?.brandName">
              <div class="label">Brand Name</div>
              <div class="value">{{ product.itemDetails?.brandName }}</div>
            </div>

            <div class="row" *ngIf="product.itemDetails?.modelNumber">
              <div class="label">Model Number</div>
              <div class="value">{{ product.itemDetails?.modelNumber }}</div>
            </div>

            <div class="row" *ngIf="product.itemDetails?.manufacturer">
              <div class="label">Manufacturer</div>
              <div class="value">{{ product.itemDetails?.manufacturer }}</div>
            </div>

            <div class="row" *ngIf="product.itemDetails?.customerReviews">
              <div class="label">Customer Reviews</div>
              <div class="value">
                <span class="flex gap-px items-start"> {{ product.itemDetails?.customerReviews?.rating }} <svg
                    xmlns="http://www.w3.org/2000/svg" fill="#EFBF04" viewBox="0 0 24 24" stroke-width="1.5"
                    stroke="none" class="size-5">
                    <path stroke-linecap="round" stroke-linejoin="round"
                      d="M11.48 3.499a.562.562 0 0 1 1.04 0l2.125 5.111a.563.563 0 0 0 .475.345l5.518.442c.499.04.701.663.321.988l-4.204 3.602a.563.563 0 0 0-.182.557l1.285 5.385a.562.562 0 0 1-.84.61l-4.725-2.885a.562.562 0 0 0-.586 0L6.982 20.54a.562.562 0 0 1-.84-.61l1.285-5.386a.562.562 0 0 0-.182-.557l-4.204-3.602a.562.562 0 0 1 .321-.988l5.518-.442a.563.563 0 0 0 .475-.345L11.48 3.5Z" />
                  </svg></span>
                ({{ product.itemDetails?.customerReviews?.totalReviews }}) <br>
                {{ product.itemDetails?.customerReviews?.ratingText }}
              </div>
            </div>

            <div class="row" *ngIf="product.itemDetails?.bestSellersRank?.length">
              <div class="label">Best Sellers Rank</div>
              <div class="value">
                <div *ngFor="let rank of product.itemDetails?.bestSellersRank">{{ rank }}</div>
              </div>
            </div>

            <!-- 🔥 Dynamic Remaining Keys (Now Beautified) -->
            <div class="row" *ngFor="let key of objectKeys(itemDetailsOtherKeys)">
              <div class="label">{{ formatKey(key) }}</div>
              <div class="value">{{ itemDetailsOtherKeys[key] }}</div>
            </div>
          </div>
        </div>
      </div>



      A+ images repated krna hhai t800 ka listing complete krna hia